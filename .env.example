# Read automatically by `docker compose` for variable substitution in
# docker-compose.yml. Copy to .env and fill in for local Docker-based dev.
# Not needed if you run backend/frontend directly with npm (they read their
# own backend/.env / frontend/.env instead).

ADMIN_USER_ID=
